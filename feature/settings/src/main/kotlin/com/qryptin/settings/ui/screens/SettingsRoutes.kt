package com.qryptin.settings.ui.screens

// ─────────────────────────────────────────────────────────────
//  SettingsRoutes  — internal to the settings nav graph
// ─────────────────────────────────────────────────────────────
object SettingsRoutes {
    const val HOME           = "settings"
    const val PROFILE        = "settings/profile"
    const val ACTIVE_SESSIONS= "settings/active_sessions"
    const val PQ_STATUS      = "settings/pq_status"
}
